#include "main.h"

/**
 * add - will add two integer and prints out result
 * @a: first int to add
 * @b: second int to add
 *
 * Return: Always 0 (success)
 */

int add(int a, int b)
{
	int c = (a + b);

	return (c);
}
